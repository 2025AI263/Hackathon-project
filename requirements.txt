# Use the CPU-only PyTorch build - much smaller download than the default
# GPU build, and Streamlit Cloud has no GPU anyway.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.38.0
opencv-python-headless==4.10.0.84
mediapipe==0.10.14
numpy==1.26.4
torch==2.3.1
torchvision==0.18.1
facenet-pytorch==2.6.0
Pillow==10.4.0
